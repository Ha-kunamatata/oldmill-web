---
interface Image {
	src: string;
	alt: string;
	caption?: string;
}

interface Props {
	images: Image[];
	columns?: 2 | 3 | 4;
}

const { images, columns = 3 } = Astro.props;

const colClass = {
	2: 'md:grid-cols-2',
	3: 'md:grid-cols-3',
	4: 'md:grid-cols-4',
}[columns];
---

<div class={`grid grid-cols-2 ${colClass} gap-3 md:gap-4`}>
	{images.map(img => (
		<figure class="aspect-square bg-border-warm rounded-lg overflow-hidden">
			<img 
				src={img.src} 
				alt={img.alt}
				loading="lazy"
				class="w-full h-full object-cover hover:scale-105 transition-transform duration-500"
			/>
		</figure>
	))}
</div>